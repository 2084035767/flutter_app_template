name: my_app_lint
description: Custom lint rules enforcing FSD constraints and ViewModel patterns.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.9.2 <4.0.0"

dependencies:
  analyzer: ">=6.0.0 <14.0.0"
  analyzer_plugin: ">=0.11.0 <0.17.0"
  analysis_server_plugin: ^0.3.14
